'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _reactRedux = require('react-redux');

// can't really test this higher order function without overwriting the require cache
/* istanbul ignore next */

exports.default = function (mapStateToProps) {
  for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  return _reactRedux.connect.apply(undefined, [function (state, ownProps) {
    var props = mapStateToProps(state, ownProps);
    var _state$await = state.await;
    var statuses = _state$await.statuses;
    var errors = _state$await.errors;

    return _extends({}, props, { statuses: statuses, errors: errors });
  }].concat(args));
};