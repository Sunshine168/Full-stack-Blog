'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _promise = require('babel-runtime/core-js/promise');

var _promise2 = _interopRequireDefault(_promise);

exports.customize = customize;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function customize(kill) {
  return function (child, signal) {
    return new _promise2.default(function (_resolve, _reject) {
      function resolve() {
        child.removeAllListeners();
        _resolve();
      }
      function reject(error) {
        child.removeAllListeners();
        _reject(error);
      }
      child.on('exit', resolve);
      child.on('error', reject);
      if (signal) kill(child, signal);else kill(child);
    });
  };
}

var kill = customize(function (child, signal) {
  if (signal != null) child.kill(signal);else child.kill();
});

exports.default = kill;